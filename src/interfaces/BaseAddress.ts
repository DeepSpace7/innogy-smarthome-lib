/**
 * Represents the configuration of the base addresses.
 */
interface BaseAddress {
  /**
   * The base address of the API endpoints.
   */
  api?: string,
  /**
   * The base address of the data endpoints.
   */
  data?: string
}